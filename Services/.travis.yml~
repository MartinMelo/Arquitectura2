language: java
jdk:
  - oraclejdk8
install:
    - "npm install -g bower"
    - "bower install"
    - "npm install"
    - "gulp build"
env:
  - NODE_ENV=travis
deploy:
  provider: openshift
  user: marianogvarela@gmail.com
  password: s2BePq54dA
  domain: vymaster
  app: cortinas
