language: java
jdk:
  - oraclejdk8
install:
    - "mvn install"
    - "mvn spring-boot:run -Dspring.profiles.active=dev"
env:
  - NODE_ENV=travis
deploy:
  provider: openshift
  user: marianogvarela@gmail.com
  password: s2BePq54dA
  domain: vymaster
  app: arqapi
