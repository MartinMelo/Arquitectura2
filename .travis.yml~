language: java
jdk:
  - oraclejdk8
sudo: false
install:
  - "mvn install"
  - "cd target"
script: java -javaagent:../newrelic.jar -jar -Dspring.profiles.active=prod api.jar

env:
  - NODE_ENV=travis
deploy:
  provider: openshift
  user: marianogvarela@gmail.com
  password: s2BePq54dA
  domain: vymaster
  app: arqapi
